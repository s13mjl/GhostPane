<Application x:Class="HideBar.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HideBar"
             xmlns:converters="clr-namespace:HideBar.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/AppleTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 全局默认样式 -->
            <Style TargetType="Window">
                <Setter Property="Background" Value="{StaticResource AppleBackgroundBrush}"/>
                <Setter Property="FontFamily" Value="{StaticResource AppleFontFamily}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="{StaticResource AppleTextPrimaryBrush}"/>
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource AppleFontFamily}"/>
                <Setter Property="Foreground" Value="{StaticResource AppleTextPrimaryBrush}"/>
            </Style>

            <!-- Bool -> Visibility 转换器 -->
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <converters:NullToVisibilityConverter x:Key="NullToVis"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
